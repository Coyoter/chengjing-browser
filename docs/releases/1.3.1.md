# 澄境瀏覽器 1.3.1

## 介面修正

還原 1.2.0 的第一層工具列設計：上一頁、下一頁、橫向天眼膠囊按鈕、收藏與三點選單。恢復原有按鈕尺寸、間距、圖文水平排列與規則數字，不再使用 1.3.0 的六格等寬配置、圓形天眼按鈕或外掛數字徽章。

分享入口只放在「三點選單 → 目前頁面 → 分享」，仍開啟 Android 系統分享面板，傳遞目前網址與標題。第一層不顯示分享按鈕。

保留 1.3.0 的 AI 設定即時更新、收藏與閱讀進度同步、啟動畫面、圖片長按下載，以及原生分享機制；不修改這些功能的底層邏輯。

## 版本與安裝

versionName：1.3.1；versionCode：20；Android 9 以上。沿用既有應用程式簽章。APK 可更新相同簽章的側載版本；AAB 供 Google Play Console 上傳，GitHub Release 不代表已提交 Google Play。Play 安裝版是否可用此 APK 覆蓋取決於 Play App Signing 憑證。

附件包含 APK、AAB、同一提交的 Source.zip、BUILD.json 與 SHA256SUMS。舊版 Release 不覆蓋。簽章、自動檢查與建置資訊以對應 Actions 與 BUILD.json 為準；真實手機的雙裝置 Drive 同步等既有實機驗收限制仍適用。
